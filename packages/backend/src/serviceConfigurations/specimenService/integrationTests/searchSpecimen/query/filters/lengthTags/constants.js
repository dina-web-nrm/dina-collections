const fieldPath = 'attributes.tags.lengthTags'
const filterFunction = 'rangeLength'
const resource = 'lengthTag'
const typeAggregationFunction = 'aggregateLengthTags'

module.exports = {
  fieldPath,
  filterFunction,
  resource,
  typeAggregationFunction,
}
