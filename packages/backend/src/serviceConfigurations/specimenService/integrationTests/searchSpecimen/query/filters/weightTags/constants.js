const filterFunction = 'rangeWeight'
const fieldPath = 'attributes.tags.weightTags'
const resource = 'weightTag'
const typeAggregationFunction = 'aggregateWeightTags'

module.exports = {
  fieldPath,
  filterFunction,
  resource,
  typeAggregationFunction,
}
