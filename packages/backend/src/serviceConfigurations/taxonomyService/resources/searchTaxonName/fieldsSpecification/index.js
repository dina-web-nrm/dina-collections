const id = require('./fields/id')
const name = require('./fields/name')
const rank = require('./fields/rank')
const rubinNumber = require('./fields/rubinNumber')

module.exports = {
  fields: [id, name, rank, rubinNumber],
}
