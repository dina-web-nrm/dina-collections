const fieldPath = 'attributes.tags.appearanceTags'
const filterFunction = 'searchAppearanceTags'
const resource = 'appearanceTag'
const typeAggregationFunction = 'aggregateAppearanceTags'

module.exports = {
  fieldPath,
  filterFunction,
  resource,
  typeAggregationFunction,
}
