import wrapStaticContentParts from '../utilities/wrapStaticContentParts'

import AddButton from './AddButton'
import TranslatedHeader from './TranslatedHeader'

export default wrapStaticContentParts({
  AddButton,
  TranslatedHeader,
})
