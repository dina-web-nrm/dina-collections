const catalogNumber = require('./catalogNumber')
const id = require('./id')
const identifier = require('./identifier')
const collectingLocation = require('./collectingLocation')

module.exports = {
  catalogNumber,
  collectingLocation,
  id,
  identifier,
}
