DONE
* repair localities (edit / create) - attributes
* make sure tests are passing
* fix performance in load specimen
* repair storage locations (attributes)

TODO

* fix load initial data
* make sure tests are passing
* go through mammal form and remove all not need ensure stuff is fetched
* getItemByTypeId -> pass relationship key



* Cleanup
  * improve resolveItemRelationship
* Next step
  * migrate taxonomy to crud module
  * remove not used files
* Merge normalized names specifications
* Use same common functions in backend
* remove normalize (build stuff backend need based on schema)

FIX
* is x-column needed?
