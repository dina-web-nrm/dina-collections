'use strict';

module.exports = {
  additionalProperties: 'ADDITIONAL_PROPERTIES',
  duplicate: 'DUPLICATE',
  enum: 'ENUM',
  maxLength: 'MAX_LENGTH',
  minLength: 'MIN_LENGTH',
  required: 'REQUIRED',
  type: 'WRONG_TYPE'
};